{% comment %}תבנית עמוד רגילה — לעמודים שלא משתמשים ב־studio{% endcomment %}
<div class="page-default" style="max-width: 42rem; margin: 2rem auto; padding: 0 1.25rem;">
  <h1 style="font-family: system-ui, sans-serif; font-size: 1.75rem; margin-bottom: 1rem;">{{ page.title }}</h1>
  <div class="rte" style="font-family: system-ui, sans-serif; line-height: 1.6;">{{ page.content }}</div>
</div>
